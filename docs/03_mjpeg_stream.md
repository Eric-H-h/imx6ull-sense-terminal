# 03 MJPEG Stream

## Goal

Serve camera frames to a browser.

## Endpoints

- `GET /`
- `GET /stream`
- `GET /status`

## Test

```sh
curl http://BOARD_IP:8080/status
```

Open in browser:

```text
http://BOARD_IP:8080/
```

## Results

TBD.

